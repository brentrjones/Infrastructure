# Core kickstart options

# Start VNC server for remote install viewing
#vnc --port 443 --password=installtime

# Start an SSH server with configured user during install time
#sshpw --username=installer installtime --plaintext
